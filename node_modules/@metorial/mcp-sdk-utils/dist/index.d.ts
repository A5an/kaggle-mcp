import { MetorialMcpSessionInit, MetorialMcpSession, MetorialMcpToolManager } from '@metorial/mcp-session';

interface McpSDK {
    mcp: {
        createSession(init: MetorialMcpSessionInit): MetorialMcpSession;
    };
}
declare let createMcpSdk: <I = void>() => <T>(handler: (d: {
    session: MetorialMcpSession;
    tools: MetorialMcpToolManager;
    input: I;
}) => Promise<T>) => ((session: MetorialMcpSession, input: I) => Promise<T>) & {
    ofSession: (session: MetorialMcpSession, input: I) => Promise<T>;
    ofSdk: (sdk: McpSDK, init: MetorialMcpSessionInit, input: I) => Promise<T>;
};

export { type McpSDK, createMcpSdk };
