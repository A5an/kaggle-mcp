'use strict';

// src/createMcpSdk.ts
var createMcpSdk = () => (handler) => {
  let ofSession = async (session, input) => {
    let tools = await session.getToolManager();
    return handler({
      session,
      tools,
      input
    });
  };
  let ofSdk = async (sdk, init, input) => {
    let session = sdk.mcp.createSession(init);
    return ofSession(session, input);
  };
  return Object.assign(ofSession, {
    ofSession,
    ofSdk
  });
};

exports.createMcpSdk = createMcpSdk;
//# sourceMappingURL=out.js.map
//# sourceMappingURL=index.cjs.map