import * as _metorial_mcp_sdk_utils from '@metorial/mcp-sdk-utils';
import * as _metorial_mcp_session from '@metorial/mcp-session';

interface MetorialOpenAiCompatibleFunctionTool {
    type: 'function';
    function: {
        name: string;
        description?: string;
        parameters: any;
        strict?: boolean;
    };
}
interface MetorialOpenAiCompatibleToolCall {
    id: string;
    type: 'function';
    function: {
        name: string;
        arguments: string;
    };
}
interface MetorialOpenAiCompatibleToolResult {
    role: 'tool';
    tool_call_id: string;
    content: string;
}
declare let createOpenAICompatibleMcpSdk: (opts?: {
    withStrict?: boolean;
}) => ((session: _metorial_mcp_session.MetorialMcpSession, input: void) => Promise<{
    tools: MetorialOpenAiCompatibleFunctionTool[];
    callTools: (calls: MetorialOpenAiCompatibleToolCall[]) => Promise<MetorialOpenAiCompatibleToolResult[]>;
}>) & {
    ofSession: (session: _metorial_mcp_session.MetorialMcpSession, input: void) => Promise<{
        tools: MetorialOpenAiCompatibleFunctionTool[];
        callTools: (calls: MetorialOpenAiCompatibleToolCall[]) => Promise<MetorialOpenAiCompatibleToolResult[]>;
    }>;
    ofSdk: (sdk: _metorial_mcp_sdk_utils.McpSDK, init: _metorial_mcp_session.MetorialMcpSessionInit, input: void) => Promise<{
        tools: MetorialOpenAiCompatibleFunctionTool[];
        callTools: (calls: MetorialOpenAiCompatibleToolCall[]) => Promise<MetorialOpenAiCompatibleToolResult[]>;
    }>;
};

export { type MetorialOpenAiCompatibleFunctionTool, type MetorialOpenAiCompatibleToolCall, type MetorialOpenAiCompatibleToolResult, createOpenAICompatibleMcpSdk };
