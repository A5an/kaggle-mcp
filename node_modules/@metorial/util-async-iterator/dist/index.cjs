"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var src_exports = {};
__export(src_exports, {
  createProgrammableAsyncIterator: () => createProgrammableAsyncIterator
});
module.exports = __toCommonJS(src_exports);

// src/asyncIterator.ts
var createProgrammableAsyncIterator = () => {
  let nextValueStack = [];
  let resolveNext = null;
  let rejectNext = null;
  let ended = false;
  let iterator = {
    [Symbol.asyncIterator]() {
      return this;
    },
    async next() {
      let nextValue = nextValueStack.shift();
      if (nextValue) {
        if (nextValue.type == "throw")
          throw nextValue.value;
        if (nextValue.type == "finish") {
          return {
            value: nextValue.value,
            done: true
          };
        }
        return {
          value: nextValue.value,
          done: false
        };
      }
      return new Promise((resolve, reject) => {
        resolveNext = resolve;
        rejectNext = reject;
      });
    }
  };
  return {
    yield(value) {
      if (ended)
        return;
      if (resolveNext) {
        resolveNext({ value, done: false });
        resolveNext = null;
      } else {
        nextValueStack.push({ type: "yield", value });
      }
    },
    throw(error) {
      if (ended)
        return;
      ended = true;
      if (rejectNext) {
        rejectNext(error);
        rejectNext = null;
      } else {
        nextValueStack.push({ type: "throw", value: error });
      }
    },
    finish(value) {
      if (ended)
        return;
      ended = true;
      if (resolveNext) {
        resolveNext({ value, done: true });
        resolveNext = null;
      } else {
        nextValueStack.push({ type: "finish", value });
      }
    },
    iterator
  };
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  createProgrammableAsyncIterator
});
//# sourceMappingURL=index.cjs.map